# Test-Project

git init
git add .
git commit -m "Initial MERN project setup"

Create the repo 

git remote add origin https://github.com/your-username/product-inventory-manager.git
git branch -M main
git push -u origin main